apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: stash-claim
  namespace: qnap-migration
spec:
  accessModes:
  - ReadOnlyMany
  storageClassName: local-storage-zfs
  volumeName: stash-pv
  resources:
    requests:
      storage: 256Mi
