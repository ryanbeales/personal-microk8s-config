apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    # It's slow when pulling models, so prevent timeouts
    nginx.ingress.kubernetes.io/proxy-send-timeout: "360s"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "360s"
    cert-manager.io/cluster-issuer: letsencryptcertmanager
  labels:
    app.kubernetes.io/name: ollama
  name: ollama
  namespace: ollama
spec:
  ingressClassName: nginx
  rules:
    - host: ollama.crobasaurusrex.ryanbeales.com
      http:
        paths:
          - backend:
              service:
                name: ollama
                port:
                  number: 11434
            path: /
            pathType: Prefix
  tls:
    - hosts:
        - ollama.crobasaurusrex.ryanbeales.com
      secretName: ollama-crobasaurusrex-ryanbeales-com-tls