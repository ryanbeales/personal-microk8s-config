apiVersion: aws.upbound.io/v1beta1
kind: ProviderConfig
metadata:
  name: upbound-aws-default
spec:
  credentials:
    source: Secret
    secretRef:
      namespace: crossplane-system
      name: aws-secret
      key: creds
