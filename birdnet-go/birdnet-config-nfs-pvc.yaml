apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: birdnet-config-nfs-pvc
  namespace: birdnet
spec:
  accessModes:
  - ReadWriteMany
  storageClassName: crobasaurusrex-nfs
  resources:
    requests:
      storage: 128Mi
